<ul class="primary-list languages">
  {{#each primaryLanguages}}
    <li>
      <a href="/more-info/{{enName}}" class="usa-button-big" data-enlang="{{enName}}">
        {{language}}
      </a>
      <small>{{enName}}</small>
    </li>
  {{/each}}
</ul>